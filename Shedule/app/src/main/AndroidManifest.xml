<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shedule" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shedule" >
        <activity
            android:name=".SheduleGroupContent"
            android:label="@string/title_activity_shedule_group_content"
            android:theme="@style/Theme.Shedule.NoActionBar" >
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Shedule.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SheduleGroupActivity"
            android:label="@string/title_activity_shedule_group"
            android:theme="@style/Theme.Shedule.NoActionBar" >
        </activity>
    </application>

</manifest>